// Package api provides the core types and interfaces shared across all
// sub-packages in the Intercom Go SDK.
//
// Key types include the Caller interface (implemented by intercom.Client),
// Result for raw HTTP responses, Iter and PagedResult for pagination,
// Filter and SearchRequest for search queries, ErrorResponse and status
// predicates for error handling, and shared domain types like TagRef,
// AdminRef, and ContactRef used across multiple services.
package api
